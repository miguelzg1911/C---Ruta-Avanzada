@model IEnumerable<SimulacroPrueba.Models.Pasajeros>
@{
    ViewData["Title"] = "Listado de Pasajeros";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Ingresar Pasajeros</a>
</p>

<table class="table table-bordered table-striped">
    <thead class="table">
    <tr>
        <th>ID</th>
        <th>Nombre Completo</th>
        <th>Documento</th>
        <th>Telefono</th>
        <th>Correo</th>
        <th>Acciones</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.NombreCompleto</td>
            <td>@item.Documento</td>
            <td>@item.Telefono</td>
            <td>@item.Correo</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Editar</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>

<a asp-controller="Menu" asp-action="Index" class="btn btn-primary">Volver</a>